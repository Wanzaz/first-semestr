#include <stdio.h>

void list_area(int a[], int n) //a[] = square brackets are empty, if you write something there, it is ignored
{
  int i;
  for(i=0;i<n;i++)
  {
    printf("%d", a[i]);
  }
  return 0;
}

int main()
{
  return 0;
}
